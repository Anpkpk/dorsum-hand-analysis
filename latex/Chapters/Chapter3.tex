% Chương 3

\chapter{PHƯƠNG PHÁP VÀ KIẾN TRÚC HỆ THỐNG}

\label{Chapter3}

%----------------------------------------------------------------------------------------

\section{Kiến trúc hệ thống tổng quát}

\begin{figure}[h]
    \centering
    \includegraphics[width=1.0\linewidth]{Figures/Chapter/3.png}
    \caption{System Pipeline}
    \label{fix:chapter1_3}
\end{figure}

% Hệ thống xử lý khối ảnh y khoa 3D được thiết kế theo mô hình luồng dữ liệu (Data Pipeline), phân chia rõ ràng các tác vụ giữa CPU (Host) và GPU (Device) nhằm tận dụng tối đa băng thông bộ nhớ và năng lực tính toán song song. Cấu trúc hệ thống bao gồm các giai đoạn và nhánh xử lý chính như sau:

Hệ thống xử lý khối ảnh y khoa 3D được thiết kế theo mô hình luồng dữ liệu (Data Pipeline), trong đó dữ liệu được tổ chức và xử lý tuần tự qua nhiều giai đoạn nhằm đảm bảo tính liên tục, hiệu quả và khả năng mở rộng của hệ thống. Kiến trúc này cho phép phân chia rõ ràng các nhiệm vụ giữa CPU (Host) và GPU (Device), tận dụng ưu điểm của từng thành phần phần cứng để đạt được hiệu năng xử lý cao đối với các tập dữ liệu ảnh y khoa có kích thước lớn.

Toàn bộ quá trình xử lý được xây dựng theo hướng tối ưu hóa việc quản lý dữ liệu, giảm thiểu thời gian truy xuất bộ nhớ và tăng cường khả năng tính toán song song. Dữ liệu ảnh đầu vào sau khi được nạp vào hệ thống sẽ trải qua các bước xử lý cần thiết trước khi được chuyển đến các mô-đun tính toán chuyên biệt. Việc phân tách các giai đoạn xử lý thành những thành phần độc lập giúp hệ thống dễ dàng bảo trì, nâng cấp và tích hợp thêm các thuật toán mới trong tương lai.

Trong kiến trúc này, CPU đóng vai trò điều phối luồng thực thi, quản lý tài nguyên và thực hiện các tác vụ điều khiển hệ thống, trong khi GPU đảm nhiệm các phép tính có độ phức tạp cao và yêu cầu khả năng xử lý song song trên khối lượng dữ liệu lớn. Cách tiếp cận này giúp khai thác hiệu quả năng lực tính toán của phần cứng hiện đại, đồng thời giảm đáng kể thời gian xử lý so với các mô hình thực thi tuần tự truyền thống.

Bên cạnh đó, hệ thống được xây dựng theo cơ chế pipeline cho phép các giai đoạn xử lý dữ liệu hoạt động liên tục và có thể chồng lấp về mặt thời gian. Nhờ vậy, quá trình xử lý ảnh y khoa 3D đạt được hiệu suất cao hơn, giảm thiểu các khoảng thời gian chờ giữa các bước xử lý và nâng cao khả năng đáp ứng đối với các ứng dụng yêu cầu tốc độ xử lý lớn trong lĩnh vực y tế và nghiên cứu hình ảnh y khoa.

Cấu trúc tổng thể của hệ thống bao gồm nhiều giai đoạn và các nhánh xử lý chuyên biệt, được tổ chức theo luồng dữ liệu thống nhất từ đầu vào đến đầu ra. Mỗi thành phần trong hệ thống đảm nhận một vai trò cụ thể và phối hợp chặt chẽ với các thành phần còn lại nhằm đảm bảo dữ liệu được xử lý chính xác, hiệu quả và ổn định trong suốt quá trình thực thi.

% Đảm bảo bạn đã thêm dòng này vào phần Preamble (trước \begin{document})
% \usepackage{enumitem}

\noindent\textbf{1. Giai đoạn 1: Tiền xử lý dữ liệu I/O}

\begin{figure}[h!]
\centering
\includegraphics[width=1\linewidth]{Figures/Chapter/4.png}
\caption{Tiền xử lý dữ liệu}
\label{fix:chapter1_4}
\end{figure}

\begin{itemize}[label={}, leftmargin=*, noitemsep, topsep=4pt]
    \item \textbf{TXT Slices:} Đọc dữ liệu đầu vào từ các lát cắt hình ảnh y khoa dưới dạng tệp văn bản (text).
    \item \textbf{Convert Binary:} Chuyển đổi dữ liệu từ định dạng văn bản sang định dạng nhị phân (Binary) để tối ưu hóa không gian lưu trữ và tăng tốc độ đọc/ghi dữ liệu.
    \item \textbf{3D Volume:} Ghép nối và tái tạo các lát cắt 2D đã được xử lý thành một khối dữ liệu không gian 3 chiều (3D Volume) hoàn chỉnh.
\end{itemize}
\vspace{0.3cm}
\noindent\textbf{2. Giai đoạn 2: Trích xuất bản đồ 2D}

\begin{figure}[h!]
\centering
\includegraphics[width=1\linewidth]{Figures/Chapter/5.png}
\caption{Trích xuất bản đồ 2D}
\label{fix:chapter1_5}
\end{figure}

\begin{itemize}[label={}, leftmargin=*, noitemsep, topsep=4pt]
    \item \textbf{Upload Volume lên VRAM:} Chuyển khối dữ liệu 3D từ bộ nhớ chính (RAM của CPU) sang bộ nhớ video (VRAM của GPU) nhằm tận dụng năng lực tính toán song song cường độ cao.
    \item \textbf{MIP Projection:} Áp dụng kỹ thuật chiếu cường độ tối đa (Maximum Intensity Projection - MIP) dọc theo một trục nhìn nhất định để làm phẳng khối 3D.
    \item \textbf{Bản đồ 2D:} Thu được kết quả đầu ra là một bản đồ 2D (2D Map) thể hiện rõ nét các cấu trúc có cường độ tín hiệu cao (như mạng lưới mạch máu).
\end{itemize}

\vspace{0.3cm}
\noindent\textbf{3. Giai đoạn 3: Xử lý song song đa nhánh}

\vspace{0.2cm}
\noindent\textbf{Nhánh 1: Vessel Analysis}

\begin{figure}[h!]
\centering
\includegraphics[width=1\linewidth]{Figures/Chapter/6.png}
\caption{Phân tích mạch máu}
\label{fix:chapter1_6}
\end{figure}

\begin{itemize}[label=$\ast$, leftmargin=*, noitemsep, topsep=4pt]
    \item \textit{Làm mờ:} Áp dụng các bộ lọc không gian để làm mịn ảnh và giảm thiểu nhiễu.
    \item \textit{Phân ngưỡng:} Sử dụng kỹ thuật phân ngưỡng (Thresholding) để phân tách rõ ràng cấu trúc mạch máu ra khỏi vùng nền.
    \item \textit{Đếm điểm ảnh:} Đếm tổng số lượng điểm ảnh (pixels) thuộc về lớp mạch máu đã được phân tách.
    \item \textit{Mật độ mạch máu:} Tính toán và xuất ra tham số định lượng về mật độ mạch máu trên toàn bộ vùng khảo sát.
\end{itemize}

\vspace{0.2cm}
\noindent\textbf{Nhánh 2: Texture Analysis}

\begin{figure}[h!]
\centering
\includegraphics[width=1\linewidth]{Figures/Chapter/7.png}
\caption{Phân tích kết cấu}
\label{fix:chapter1_7}
\end{figure}

\begin{itemize}[label=$\ast$, leftmargin=*, noitemsep, topsep=4pt]
    \item \textit{Khởi tạo GLCM:} Xây dựng ma trận đồng mức xám (Gray-Level Co-occurrence Matrix - GLCM) từ bản đồ 2D để đánh giá mối quan hệ không gian giữa các mức xám.
    \item \textit{Trích xuất đặc trưng:} Dựa trên GLCM, hệ thống tiến hành tính toán và trích xuất 4 đặc trưng thống kê kết cấu cốt lõi bao gồm: \textbf{Contrast} (Độ tương phản), \textbf{Energy} (Năng lượng), \textbf{Homogeneity} (Độ đồng nhất) và \textbf{Correlation} (Độ tương quan).
\end{itemize}


% TODO: Vẽ một Sơ đồ khối ba giai đoạn trên

%----------------------------------------------------------------------------------------

\section{Tối ưu hóa I/O Dữ liệu}

Một trong những nút thắt cổ chai lớn nhất của hệ thống ban đầu là quá trình nạp dữ liệu. Dữ liệu gốc được lưu dưới dạng hàng trăm tệp văn bản tĩnh (\code{.txt}), chứa các giá trị mức xám dưới dạng chuỗi ký tự ASCII. Việc yêu cầu CPU mở từng tệp, đọc chuỗi, giải mã thành số thực (float) và chuẩn hóa màu sắc mất rất nhiều thời gian.

Để giải quyết vấn đề này, một module Tiền xử lý dữ liệu (Preprocessor) được xây dựng trên C++. Module này thực hiện các bước sau:

\begin{itemize}
    \item Quét qua toàn bộ các tệp \code{.txt}.
    \item Nén và xuất toàn bộ dữ liệu ra một tệp nhị phân duy nhất (\code{volume\_3d.raw}).
\end{itemize}

Nhờ định dạng Binary \code{.raw}, máy tính có thể nạp hàng tỷ byte dữ liệu trực tiếp vào bộ nhớ RAM và đẩy lên VRAM của GPU trong thời gian ngắn.

%----------------------------------------------------------------------------------------

\section{Thiết kế và Triển khai các hàm Kernel CUDA}

Để thực thi song song trên GPU, mạng lưới luồng (Grid) được thiết kế dựa trên kích thước của bề mặt ảnh 2D ($W \times H$). Cụ thể, ảnh được chia thành các Block nhỏ kích thước $16 \times 16$ threads. Số lượng Block được tự động tính toán bù trừ để đảm bảo phủ kín toàn bộ diện tích ảnh mà không bị tràn viền.



\subsection{Kernel Chiếu ảnh cường độ tối đa}

Thuật toán MIP duyệt qua toàn bộ chiều sâu $D$ của khối ảnh. Trong không gian 3D, mỗi luồng (thread) được gắn tọa độ $(x, y)$ tương ứng với một điểm ảnh trên mặt phẳng đích. GPU phân công mỗi luồng chỉ chạy 1 vòng lặp dọc theo trục $Z$. Luồng sẽ duyệt qua các giá trị tại tọa độ $(x, y, z)$ tương ứng, so sánh và lưu trữ giá trị cực đại tìm được vào biến \code{max\_val}, sau đó ghi thẳng vào mảng \code{d\_mip} 2D.

\subsection{Các Kernel thuộc nhánh Vessel Analysis}

\begin{itemize}
    \item \textbf{Kernel Làm mờ :} Mỗi luồng tại tọa độ $(x, y)$ kiểm tra các điều kiện biên để tránh lỗi truy xuất vùng nhớ (Segmentation fault). Nếu an toàn, luồng sẽ tính tổng cường độ của điểm ảnh đó và 8 điểm lân cận, sau đó chia cho 9 để ra giá trị trung bình và ghi vào ảnh \code{d\_blurred}.
    
    \item \textbf{Kernel Phân ngưỡng :} Các luồng hoạt động hoàn toàn độc lập, thực hiện phép toán rẽ nhánh: Nếu giá trị tại $(x,y)$ vượt qua ngưỡng được cài đặt (ví dụ: 140), gán bằng 255; ngược lại gán bằng 0.
    
    \item \textbf{Kernel Đếm mạch máu :} Để tránh việc hàng ngàn luồng ghi đè lên nhau khi cộng dồn tổng số điểm ảnh trắng, lệnh khóa bộ nhớ \code{atomicAdd()} được sử dụng. Mỗi luồng phát hiện điểm mạch máu sẽ an toàn cộng thêm 1 vào biến con trỏ \code{d\_white\_count} lưu trên VRAM.
\end{itemize}

\subsection{Kernel thuộc nhánh Texture Analysis}

Khác với nhánh trên, tính toán kết cấu yêu cầu đếm tần suất xuất hiện của các cặp điểm ảnh lân cận. Tại hàm \code{Compute\_GLCM}, mỗi luồng xử lý một cặp điểm ảnh (\code{pixel\_1}, \code{pixel\_2}) theo độ dời ngang ($dx = 1, dy = 0$).

Ma trận GLCM $256 \times 256$ được trải phẳng thành mảng 1 chiều kích thước 65.536 phần tử. Chỉ số để cập nhật ma trận được tính toán bằng công thức:
\begin{equation}
    \text{index} = \text{pixel\_1} \times 256 + \text{pixel\_2}
    \label{eq:glcm_index}
\end{equation}

Tại chỉ số này, luồng tiếp tục gọi \code{atomicAdd()} để tăng tần suất cặp mức xám lên 1 đơn vị.

%----------------------------------------------------------------------------------------

\section{Quản lý bộ nhớ và Đồng bộ hóa}

\begin{itemize}
    \item \textbf{Cấp phát:} Sử dụng \code{cudaMalloc} để cấp phát vùng nhớ độc lập trên VRAM cho từng giai đoạn (Khối gốc, Ảnh MIP, Ảnh Blur, Ảnh nhị phân và Ma trận GLCM).
    
    \item \textbf{Làm sạch bộ nhớ:} Trước khi khởi chạy các Kernel đếm tổng hoặc GLCM, bộ nhớ VRAM luôn chứa dữ liệu rác. Hàm \code{cudaMemset} được gọi để ép các vùng nhớ này về 0, đảm bảo tính chính xác tuyệt đối cho \code{atomicAdd}.
    
    \item \textbf{Đồng bộ hóa:} Giữa mỗi bước thực thi Kernel, lệnh \code{cudaDeviceSynchronize()} đóng vai trò là rào chắn (Barrier), bắt buộc CPU phải tạm dừng để chờ toàn bộ hàng chục ngàn luồng của GPU hoàn thành xong công việc hiện tại, mới được phép phát lệnh chạy Kernel tiếp theo, tránh tình trạng lấy kết quả sai lệch.
\end{itemize}

%----------------------------------------------------------------------------------------

